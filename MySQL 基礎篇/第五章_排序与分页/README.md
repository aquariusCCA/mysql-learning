# 第五章_排序与分页

这一章开始整理 MySQL 查询结果的顺序控制与分页处理。排序用于让查询结果按指定字段稳定展示；分页则用于控制一次返回多少数据，以及从哪一段结果开始返回。

## 本章在整体学习地图中的位置

- 第三章主要建立了 `SELECT`、`WHERE` 和表结构查看的基础查询骨架。
- 第四章补上了表达式和条件判断所需的运算符。
- 这一章开始处理查询结果“怎么展示”的问题，也就是排序和后续分页；当前仓库内容以排序为主，分页主题仍可继续补充。

## 建议阅读顺序

1. [1 排序数据](./1%20排序数据.md)：先掌握 `ORDER BY` 的基本写法、升降序、单列排序和多列排序。
2. [补充：根据别名排序](./根據別名排序.md)：需要专门回查别名排序或 `ORDER BY 3` 时再看这篇补充。
3. 后续补齐分页内容后，再继续学习 `LIMIT` 与排序结合使用的场景。

## 本章小节

- [1 排序数据](./1%20排序数据.md)：整理 `ORDER BY` 的基本规则、别名排序、位置排序和多列排序。
- [补充：根据别名排序](./根據別名排序.md)：专门补充列别名排序与字段位置排序的差异。

## 本章适合快速回查的主题

- 想确认升序、降序写法：看 [1 排序数据](./1%20排序数据.md)
- 想确认 `ORDER BY` 是否可以使用列别名：看 [1 排序数据](./1%20排序数据.md) 或 [补充：根据别名排序](./根據別名排序.md)
- 想确认 `ORDER BY 3` 代表什么、为什么不推荐：看 [补充：根据别名排序](./根據別名排序.md)
- 想确认多列排序的字段优先级：看 [1 排序数据](./1%20排序数据.md)
- 想确认不写 `ORDER BY` 时能不能依赖默认顺序：看 [1 排序数据](./1%20排序数据.md)

## 返回导航

- [回到 README](../../README.md)
- [上一章：第四章_运算符](../第四章_运算符/README.md)
