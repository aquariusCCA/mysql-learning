# 7 常见问题的解决（课外内容）

> 所属章节：MySQL 基礎篇 / 第二章_MySQL環境搭建
> 建议回查情境：忘记 `root` 密码、`mysql` 命令无法识别、执行 SQL 时没有选中数据库、插入中文时报字符集错误、需要修改数据库或表的字符编码时
> 上一节：[6 MySQL 目录结构与源码](./6%20MySQL%20目录结构与源码.md)

## 本节导读

这一节集中整理 MySQL 初学阶段最常见的几类问题，例如忘记 `root` 密码、命令无法识别、没有选中数据库，以及字符集导致的中文乱码或报错。

它更像故障排查索引，而不是顺序阅读型内容。平时不需要从头读到尾，遇到具体问题时，直接按错误现象、错误编号或关键字跳到对应小节会更高效。

如果你想先看这些问题分别是在什么操作场景下出现的，可以搭配 [2 MySQL 的下载、安装、配置](./2%20MySQL%20的下载、安装、配置.md)、[3 MySQL 的登录](./3%20MySQL%20的登录.md) 和 [4 MySQL 演示使用](./4%20MySQL%20演示使用.md) 一起看。

## 你会在这篇学到什么

- 忘记 `root` 密码时，如何通过跳过权限表检查来重置密码。
- `mysql` 命令无法识别时，为什么通常要检查 `Path` 环境变量。
- 遇到 `ERROR 1046 (3D000): No database selected` 时，如何选择默认数据库。
- 命令行客户端插入中文时报 `ERROR 1366` 时，如何从字符集角度排查。
- 修改 `my.ini` 后，为什么已有数据库和表可能仍然保留旧字符编码。
- 修改数据库、表和字段字符编码时，分别应该使用哪些 `ALTER` 语句。

## 快速定位

- `问题1`：忘记 `root` 密码，无法正常登录
- `问题2`：`mysql` 命令提示不是内部或外部命令
- `问题3`：出现 `ERROR 1046 (3D000): No database selected`
- `问题4`：命令行插入中文时报 `ERROR 1366 (HY000)`
- `问题5`：修改数据库、表、字段字符编码

## 关键字

- `root 密码重置`：忘记 `root` 密码时的处理思路
- `mysqld`：MySQL 服务进程，可用特殊参数启动
- `--skip-grant-tables`：跳过权限表校验，常用于紧急重置密码
- `Path`：`mysql` 命令无法识别时需检查的环境变量
- `ERROR 1046 (3D000)`：没有选择数据库
- `USE 数据库名`：切换当前默认数据库
- `character_set_%`：查看当前字符集相关配置
- `SET NAMES GBK`：设置当前连接的客户端字符集
- `my.ini`：MySQL 配置文件，可修改默认字符编码
- `latin1` `utf8`：数据库、表、字段常见字符编码
- `ALTER DATABASE`：修改数据库字符编码
- `ALTER TABLE`：修改表或字段字符编码

## 建议阅读顺序

- 如果你是按报错排查，优先根据错误现象直接跳到对应问题，不需要从头顺读。
- 如果你是在整理自己的环境，建议先看 `问题2`、`问题3`、`问题4`，这些最常见。
- 如果你的重点是字符集与乱码问题，先看 `问题4`，再看 `问题5`，因为它们对应的是“当前连接字符集”和“数据库对象字符编码”两个不同层级。

## 问题1：root 用户密码忘记，重置的操作

### 现象

忘记 `root` 用户密码，导致无法通过正常密码登录 MySQL。

### 处理步骤

1. 通过任务管理器或者服务管理，先关闭 `mysqld` 服务进程。
2. 通过命令行使用特殊参数启动 `mysqld`：

   ```bash
   shell mysqld --defaults-file="D:\\ProgramFiles\\mysql\\MySQLServer5.7Data\\my.ini" --skip-grant-tables
   ```

3. 此时 `mysqld` 已启动，并且暂时跳过权限检查。
4. 另开一个客户端，使用 `mysql -uroot` 无密码登录。
5. 修改权限表：

   ```sql
   use mysql;
   update user set authentication_string=password('新密码') where user='root' and Host='localhost';
   flush privileges;
   ```

6. 再次关闭这个特殊启动的 `mysqld` 进程。
7. 按正常方式重新启动 MySQL 服务。
8. 使用新密码重新登录。

### 注意点

`--skip-grant-tables` 会跳过权限表检查，只适合用于紧急重置密码。密码修改完成后，应关闭这种特殊启动方式，恢复正常服务启动。

## 问题2：mysql 命令报“不是内部或外部命令”

### 现象

在命令行输入 `mysql` 后，提示“不是内部或外部命令”。

### 处理方式

这通常表示系统还找不到 MySQL 的命令行程序。把 MySQL 安装目录下的 `bin` 目录加入环境变量 `Path` 即可。

![mysql 命令无法识别示例](./images/image-20210914093150145.png)

### 注意点

- 配置完成后，需要重新打开命令行窗口，让新的环境变量生效。
- 如果你不确定 `bin` 目录的位置，可以回看 [2 MySQL 的下载、安装、配置](./2%20MySQL%20的下载、安装、配置.md) 和 [6 MySQL 目录结构与源码](./6%20MySQL%20目录结构与源码.md)。

## 问题3：错误 `ERROR 1046 (3D000)`，没有选择数据库

### 现象

执行表操作或数据操作时出现下面的错误：

```text
ERROR 1046 (3D000): No database selected
```

这表示当前会话还没有选中默认数据库。

### 处理方式

解决方案 1：使用 `USE 数据库名;`

```sql
USE 数据库名;
```

执行后，后续 SQL 默认会针对这个数据库操作。

解决方案 2：在表对象前面加上数据库名，例如：

```sql
数据库名.表名
```

### 注意点

- 如果后续很多 SQL 都在同一个数据库下执行，优先使用 `USE 数据库名;`。
- 如果只是临时访问另一个数据库里的某一张表，用 `数据库名.表名` 更灵活。

## 问题4：命令行客户端的字符集问题

### 现象

在命令行客户端插入中文数据时，出现字符集错误：

```text
mysql> INSERT INTO t_stu VALUES(1,'张三','男');
ERROR 1366 (HY000): Incorrect string value: '\\xD5\\xC5\\xC8\\xFD' for column 'sname' at row 1
```

### 原因

常见原因是：服务器端认为当前客户端字符集是 `utf-8`，而你实际输入数据时使用的环境却更接近 `GBK`。

![字符集不一致示意](./images/1554912924219.png)

### 排查与处理

先查看当前字符集配置：

```sql
SHOW VARIABLES LIKE 'character_set_%';
```

![查看字符集配置](./images/1554912943186.png)

如果确认是当前连接字符集不匹配，可以先尝试设置当前连接的客户端字符集：

```sql
SET NAMES GBK;
```

![设置当前连接字符集](./images/1554912957353.png)

### 注意点

- `SET NAMES GBK` 主要影响当前连接。
- 如果重新打开命令行客户端，可能需要重新设置。
- 如果问题反复出现，就不能只停留在连接级别，还要继续检查 `my.ini`、数据库、数据表和字段本身的字符编码。

## 问题5：修改数据库和表的字符编码

### 修改编码的基本流程

1. 停止 MySQL 服务。
2. 修改 `my.ini` 文件。
3. 重新启动服务。

### 为什么修改 `my.ini` 之后问题可能还在

如果数据库和数据表是在修改 `my.ini` 之前创建的，那么它们原有的字符编码仍然会保留。例如，原来是 `latin1`，并不会因为你改了配置文件就自动变成 `utf8`。

这时通常有两种思路：

- 删除旧对象后重新创建
- 使用 `ALTER` 语句逐层修改

### 示例：原来使用 latin1 创建数据库和表

```sql
mysql> create database 0728db charset Latin1;
Query OK, 1 row affected (0.00 sec)
```

```sql
mysql> use 0728db;
Database changed
```

```sql
mysql> create table student (id int , name varchar(20)) charset Latin1;
Query OK, 0 rows affected (0.02 sec)

mysql> show create table student\G
*************************** 1. row ***************************
       Table: student
Create Table: CREATE TABLE `student` (
  `id` int(11) NOT NULL,
  `name` varchar(20) DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=latin1
1 row in set (0.00 sec)
```

### 示例：修改表和字段字符编码

先修改表的默认字符编码：

```sql
mysql> alter table student charset utf8;
Query OK, 0 rows affected (0.01 sec)
Records: 0  Duplicates: 0  Warnings: 0

mysql> show create table student\G
*************************** 1. row ***************************
       Table: student
Create Table: CREATE TABLE `student` (
  `id` int(11) NOT NULL,
  `name` varchar(20) CHARACTER SET latin1 DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8
1 row in set (0.00 sec)
```

这一步只改了表的默认字符编码，但已有字段 `name` 仍然保持 `latin1`，所以还需要单独修改字段：

```sql
mysql> alter table student modify name varchar(20) charset utf8;
Query OK, 0 rows affected (0.05 sec)
Records: 0  Duplicates: 0  Warnings: 0

mysql> show create table student\G
*************************** 1. row ***************************
       Table: student
Create Table: CREATE TABLE `student` (
  `id` int(11) NOT NULL,
  `name` varchar(20) DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8
1 row in set (0.00 sec)
```

### 示例：修改数据库字符编码

```sql
mysql> show create database 0728db;
+--------+-----------------------------------------------------------------+
|Database| Create Database                                                 |
+--------+-----------------------------------------------------------------+
| 0728db | CREATE DATABASE `0728db` /*!40100 DEFAULT CHARACTER SET latin1 */ |
+--------+-----------------------------------------------------------------+
1 row in set (0.00 sec)

mysql> alter database 0728db charset utf8;
Query OK, 1 row affected (0.00 sec)

mysql> show create database 0728db;
+--------+-----------------------------------------------------------------+
|Database| Create Database                                                 |
+--------+-----------------------------------------------------------------+
| 0728db | CREATE DATABASE `0728db` /*!40100 DEFAULT CHARACTER SET utf8 */ |
+--------+-----------------------------------------------------------------+
1 row in set (0.00 sec)
```

### 注意点

修改数据库默认字符编码、修改表默认字符编码、修改字段字符编码，是三个不同层级：

- `ALTER DATABASE`：改数据库级默认值
- `ALTER TABLE ... CHARSET ...`：改表级默认值
- `ALTER TABLE ... MODIFY ... CHARSET ...`：改字段级编码

已有字段不会因为表默认字符编码改变就自动改变，所以排查乱码问题时，要分别确认数据库、表和字段。

## 常见回查问题

- 忘记 `root` 密码时，应该用哪个参数临时跳过权限表检查？
- `mysql` 命令提示不是内部或外部命令时，应该检查哪个环境变量？
- `ERROR 1046 (3D000)` 表示什么？应该用哪条语句选择数据库？
- 插入中文时报 `ERROR 1366` 时，为什么要检查客户端字符集？
- `SET NAMES GBK` 解决的是当前连接的问题，还是所有连接的问题？
- 修改表默认字符编码后，为什么字段仍然可能保持 `latin1`？
- 修改数据库、表、字段字符编码分别应该使用哪些语句？

## 延伸阅读

- [2 MySQL 的下载、安装、配置](./2%20MySQL%20的下载、安装、配置.md)
- [3 MySQL 的登录](./3%20MySQL%20的登录.md)
- [4 MySQL 演示使用](./4%20MySQL%20演示使用.md)
