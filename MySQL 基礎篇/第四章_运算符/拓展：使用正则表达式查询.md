# 拓展：使用正则表达式查询

> 所属章节：[第四章_运算符](./README.md)
> 建议回查情境：已经知道 `REGEXP` 能做正则匹配，但忘了常见符号写法，或需要快速拼出字符串模式查询时
> 前置阅读：[2 比较运算符](./2%20比较运算符.md)
> 下一章：[第五章_排序与分页](../第五章_排序与分页/README.md)

## 本节导读

这一节是 `REGEXP` 运算符的延伸笔记，重点不是再解释什么是比较运算符，而是集中整理“正则表达式到底怎么写”。当你已经知道 `REGEXP` 可以做比 `LIKE` 更复杂的匹配后，这篇就是实际查符号、看模式、套用查询语句的速查页。

第一次阅读时，建议先看“常用匹配符号速查表”，先建立每个符号的大致用途，再按后面的例子理解开头匹配、结尾匹配、单字符匹配、重复次数与字符集合这些常见写法。复习时，可以直接跳到你需要的那一种模式。

## 你会在这篇学到什么

- `REGEXP` 适合解决哪些 `LIKE` 不方便处理的字符串匹配问题。
- `^`、`$`、`.`、`[]`、`[^]`、`*`、`+`、`?`、`{n,m}` 这些符号分别代表什么。
- 如何把正则模式写进 `WHERE ... REGEXP 'pattern'` 查询条件。
- 如何根据“开头、结尾、包含、排除、连续出现次数”这些需求快速选出合适的模式。

## 快速定位

- `常用匹配符号速查表`：先确认每个正则符号的用途。
- `1. 查询以特定字符或字符串开头的记录`：看 `^`。
- `2. 查询以特定字符或字符串结尾的记录`：看 `$`。
- `3. 用符号 "." 来替代字符串中的任意一个字符`：看 `.`。
- `4. 使用 "*" 和 "+" 来匹配多个字符`：看重复出现的最常见写法。
- `5. 匹配指定字符串`：看普通字符串和 `|` 的组合匹配。
- `6. 匹配指定字符中的任意一个`：看 `[]`。
- `7. 匹配指定字符以外的字符`：看 `[^]`。
- `8. 使用 "{n,}" 或者 "{n,m}" 来指定字符串连续出现的次数`：看次数控制。

## 建议阅读顺序

- 第一次学习时，建议按“速查表 -> 开头 / 结尾 -> 单字符 -> 重复次数 -> 字符集合”的顺序阅读。
- 如果你现在的需求只是“名字以某个字母开头”或“以某个字母结尾”，先看第 `1`、`2` 节即可。
- 如果你写的是复杂字符串筛选，建议先回到 [2 比较运算符](./2%20比较运算符.md) 看 `LIKE` 和 `REGEXP` 的区别，再回来选正则写法。
- 如果你只是忘了某个正则符号的意义，直接看“常用匹配符号速查表”。

## 关键字

- `REGEXP`：在 MySQL 中使用正则表达式做模式匹配的运算符。
- `正则表达式`：用一套特殊符号描述字符串匹配规则的写法。
- `^`：匹配字符串开头。
- `$`：匹配字符串结尾。
- `.`：匹配任意单个字符。
- `[]`：匹配字符集合中的任意一个字符。
- `[^]`：匹配不在字符集合中的字符。
- `*`、`+`、`?`：控制前一个字符出现的次数。
- `{n}`、`{n,}`、`{n,m}`：指定前一个字符或模式出现的精确次数或范围。

## 常用匹配符号速查表

正则表达式通常被用来检索或替换那些符合某个模式的文本内容，根据指定的匹配模式匹配文本中符合要求的特殊字符串。例如，从一个文本文件中提取电话号码、查找一篇文章中重复的单词，或者替换用户输入的某些敏感词语等，这些场景都可以使用正则表达式。正则表达式强大而且灵活，可以应用于非常复杂的查询。

MySQL 中使用 `REGEXP` 关键字指定正则表达式的字符匹配模式。下表列出了 `REGEXP` 操作符中常用的字符匹配写法：

| 符号 | 说明 | 示例 |
| --- | --- | --- |
| `^` | 匹配字符串开头 | `'^abc'` 匹配以 `"abc"` 开头的字符串 |
| `$` | 匹配字符串结尾 | `'xyz$'` 匹配以 `"xyz"` 结尾的字符串 |
| `.` | 匹配任意单个字符 | `'a.b'` 匹配 `"acb"`、`"a1b"`、`"a_b"` |
| `[abc]` | 匹配括号中的任意字符 | `'[aeiou]'` 匹配任何元音字母 |
| `[a-z]` | 匹配指定范围的字符 | `'[0-9]'` 匹配任何数字 |
| `[^abc]` | 排除括号内的字符 | `'[^aeiou]'` 匹配非元音字母 |
| `a*` | 匹配 `0` 个或多个 `a` | `'ab*'` 匹配 `"a"`、`"ab"`、`"abb"` |
| `a+` | 匹配 `1` 个或多个 `a` | `'ab+'` 匹配 `"ab"`、`"abb"`，但不匹配 `"a"` |
| `a?` | 匹配 `0` 个或 `1` 个 `a` | `'ab?'` 匹配 `"a"`、`"ab"` |
| `a{n}` | 恰好匹配 `n` 次 `a` | `'a{2}'` 匹配 `"aa"`，但不匹配 `"a"` 或 `"aaa"` |
| `a{n,}` | 至少匹配 `n` 次 `a` | `'a{2,}'` 匹配 `"aa"`、`"aaa"`、`"aaaa"` |
| `a{n,m}` | 匹配 `n` 到 `m` 次 `a` | `'a{2,4}'` 匹配 `"aa"`、`"aaa"`、`"aaaa"` |

### 回查提示

如果你只记得“`REGEXP` 可以写很复杂的模式”，但忘了某个符号具体是什么意思，先回到这张表。

## 1. 查询以特定字符或字符串开头的记录

字符 `^` 匹配以特定字符或者字符串开头的文本。

在 `fruits` 表中，查询 `f_name` 字段以字母 `b` 开头的记录：

```sql
SELECT * FROM fruits WHERE f_name REGEXP '^b';
```

### 回查提示

只要你的需求里出现“以某个字母开头”或“前缀必须是某段文本”，优先想到 `^`。

## 2. 查询以特定字符或字符串结尾的记录

字符 `$` 匹配以特定字符或者字符串结尾的文本。

在 `fruits` 表中，查询 `f_name` 字段以字母 `y` 结尾的记录：

```sql
SELECT * FROM fruits WHERE f_name REGEXP 'y$';
```

### 回查提示

如果你的条件是“后缀必须是什么”，就检查模式结尾有没有写 `$`。

## 3. 用符号 "." 来替代字符串中的任意一个字符

字符 `.` 匹配任意一个字符。

在 `fruits` 表中，查询 `f_name` 字段值包含字母 `a` 与 `g`，且两个字母之间只有一个字符的记录：

```sql
SELECT * FROM fruits WHERE f_name REGEXP 'a.g';
```

### 回查提示

当你确定某个位置“有一个字符，但不在乎它是谁”时，就看 `.`。

## 4. 使用 "*" 和 "+" 来匹配多个字符

- 星号 `*` 匹配前面的字符任意多次，包括 `0` 次。
- 加号 `+` 匹配前面的字符至少一次。

在 `fruits` 表中，查询 `f_name` 字段值以字母 `b` 开头，且 `b` 后面出现字母 `a` 的记录：

```sql
SELECT * FROM fruits WHERE f_name REGEXP '^ba*';
```

在 `fruits` 表中，查询 `f_name` 字段值以字母 `b` 开头，且 `b` 后面出现字母 `a` 至少一次的记录：

```sql
SELECT * FROM fruits WHERE f_name REGEXP '^ba+';
```

### 回查提示

看到“可以没有”就考虑 `*`，看到“至少要有一次”就考虑 `+`。

## 5. 匹配指定字符串

正则表达式可以匹配指定字符串，只要这个字符串在查询文本中即可；如果要匹配多个字符串，多个字符串之间使用分隔符 `|` 隔开。

在 `fruits` 表中，查询 `f_name` 字段值包含字符串 `on` 的记录：

```sql
SELECT * FROM fruits WHERE f_name REGEXP 'on';
```

在 `fruits` 表中，查询 `f_name` 字段值包含字符串 `on` 或者 `ap` 的记录：

```sql
SELECT * FROM fruits WHERE f_name REGEXP 'on|ap';
```

### 回查提示

如果你的需求是“命中这个词，或者命中另一个词”，就可以从 `|` 开始想。

## 6. 匹配指定字符中的任意一个

方括号 `[]` 指定一个字符集合，只匹配其中任何一个字符。

在 `fruits` 表中，查找 `f_name` 字段中包含字母 `o` 或者 `t` 的记录：

```sql
SELECT * FROM fruits WHERE f_name REGEXP '[ot]';
```

在 `fruits` 表中，查询 `s_id` 字段中包含 `4`、`5` 或者 `6` 的记录：

```sql
SELECT * FROM fruits WHERE s_id REGEXP '[456]';
```

### 回查提示

只要你的条件是“几个字符里任意一个都可以”，就优先看 `[]`。

## 7. 匹配指定字符以外的字符

`[^字符集合]` 匹配不在指定集合中的任何字符。

在 `fruits` 表中，查询 `f_id` 字段中包含字母 `a~e` 和数字 `1~2` 以外字符的记录：

```sql
SELECT * FROM fruits WHERE f_id REGEXP '[^a-e1-2]';
```

### 回查提示

当需求变成“不要这些字符”，就从 `[^...]` 开始写，而不是继续堆普通字符集合。

## 8. 使用 `{n,}` 或者 `{n,m}` 来指定字符串连续出现的次数

- `{n,}` 表示至少匹配 `n` 次前面的字符。
- `{n,m}` 表示匹配前面的字符不少于 `n` 次，不多于 `m` 次。

在 `fruits` 表中，查询 `f_name` 字段值出现字母 `x` 至少 `2` 次的记录：

```sql
SELECT * FROM fruits WHERE f_name REGEXP 'x{2,}';
```

在 `fruits` 表中，查询 `f_name` 字段值出现字符串 `ba` 最少 `1` 次、最多 `3` 次的记录：

```sql
SELECT * FROM fruits WHERE f_name REGEXP 'ba{1,3}';
```

### 回查提示

如果你的需求里出现“至少几次”“最多几次”“连续出现多少次”，直接想到花括号次数控制。

## 常见混淆点

- `LIKE` 更适合简单前后缀或模糊匹配；当规则变成“字符集合、次数控制、排除字符”时，`REGEXP` 更合适。
- 正则里的很多符号都有特殊含义，阅读模式时不要把它们当普通字符。
- 写复杂模式时，建议先用自然语言说清楚需求，再翻成正则，这样比较不容易写错。
- 这篇示例主要帮助你理解写法；真正上手时，仍然要结合你自己的表结构和字段类型判断。

## 本节小结

- 这是一篇 `REGEXP` 的速查型扩展笔记，重点是把常见符号和查询例子集中放在一起。
- 最常用的一组符号是 `^`、`$`、`.`、`[]`、`*`、`+` 与 `{n,m}`。
- 如果你只需要简单模糊匹配，先考虑 `LIKE`；如果需要更复杂的字符串规则，再回到这篇使用 `REGEXP`。
